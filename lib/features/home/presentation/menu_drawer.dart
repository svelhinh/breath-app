import 'package:flutter/material.dart';

/// Menu drawer for navigation and app options.
///
/// This menu drawer will give the user access to general app destinations
/// such as settings, terms of service, privacy policy, contact/support, and more.
class MenuDrawer extends StatelessWidget {
  /// Creates a new instance of [MenuDrawer].
  const MenuDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
